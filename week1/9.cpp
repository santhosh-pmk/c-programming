#include<stdio.h>
#include<math.h>
int main(){
	float a;
	printf("enter a number to find the square root:");
	scanf("%f",&a);
	printf("the square root of the given number is: %f",sqrt(a));
	return 0;
}
